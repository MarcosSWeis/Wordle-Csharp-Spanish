<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand m-auto" href="">Wordle</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link  justify-content-around h2" href="" Match="NavLinkMatch.All">
                <i class="bi bi-house-door"></i>Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link d-flex justify-content-around h2" href="settings">
                <i class="bi bi-gear-wide"></i> Ajustes
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link d-flex justify-content-around h2" href="wordle" >
                <i class="bi bi-controller"></i>Wordle
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
